                ┌──────────────────────────┐
                │        User Input        │
                │  - Upload CSV file       │
                │  - Type natural query    │
                └────────────┬─────────────┘
                             │
                             ▼
                ┌──────────────────────────┐
                │      Preprocessing       │
                │ - Read CSV, show preview │
                │ - Extract metadata       │
                └────────────┬─────────────┘
                             │
                             ▼
                ┌──────────────────────────┐
                │  Query Understanding     │
                │  1. LLM (flan-t5-base)   │
                │  2. Rule-based fallback  │
                └────────────┬─────────────┘
                             │
                             ▼
                ┌──────────────────────────┐
                │ Operation Suggestion     │
                │ - Generate 2–3 options   │
                │ - User confirms/refines  │
                └────────────┬─────────────┘
                             │
                             ▼
                ┌──────────────────────────┐
                │  Data Operations Engine  │
                │ - Filter / Sort / Group  │
                │ - Aggregate / Pivot      │
                └────────────┬─────────────┘
                             │
                             ▼
                ┌──────────────────────────┐
                │   Visualization & View   │
                │ - Charts (line, bar etc.)│
                │ - Data table updates     │
                │ - Explanation text       │
                └────────────┬─────────────┘
                             │
                             ▼
                ┌──────────────────────────┐
                │   Export & Download      │
                │ - CSV export             │
                │ - Chart/image download   │
                └──────────────────────────┘





Complete Project Structure

data-explorer-with-natural-commands/
├── App.py                        # Main Streamlit app entry point
├── config.py                     # Configuration file (app settings)
├── nlp_manager.py                # Handles NLP query processing
├── prompt.py                     # Prompt templates for LLM + rules
├── projetc_visul.txt             # Project notes / visualization plan
├── requirements.txt              # Project dependencies
├── README.md                     # Documentation and usage guide
├── .gitignore                    # Ignore venv/cache files
│
├── data/                         # Sample datasets (CSV/TXT)
│
├── exports/                      # Exported files from app
│   └── session_state.json        # Persisted session state
│
├── nlp_module/                   # NLP processing logic
│   ├── __init__.py               # Marks folder as Python package
│   ├── config.py                 # NLP-specific configs
│   ├── core/                     # Core parsing logic
│   │   ├── parser.py             # Natural language → operations
│   │   ├── __init__.py
│   │   └── __pycache__/          # Cache files
│   ├── formatters/               # Formatting responses for UI
│   │   ├── ui_formatter.py       # Prepares clean text for frontend
│   │   ├── __init__.py
│   │   └── __pycache__/          
│   └── __pycache__/              
│
├── __pycache__/                  # Python cache files
│
└── venv/                         # Virtual environment (not for submission)
